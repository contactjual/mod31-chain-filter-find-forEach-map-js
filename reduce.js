const numbers = [4, 56, 7, 9, 7];
const total = numbers.reduce((previous, current) => previous + current, 0)
console.log(total);

// previous - protita sonka jog kore kore jog fol 
// current - next sonka ti